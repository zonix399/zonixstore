import React from 'react';

const Cart: React.FC = () => {
    return (
        <div>
            <h1>Your Shopping Cart</h1>
            <p>Your cart is currently empty.</p>
            {/* Add functionality to display cart items and manage them */}
        </div>
    );
};

export default Cart;